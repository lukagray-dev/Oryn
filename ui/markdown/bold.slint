// =============================================================================
// Markdown Bold Token (`ui/markdown/bold.slint`)
// =============================================================================
// Defines global styling tokens and font weight parameters for bold text formatting.

export global BoldTokens {
    in property <int> bold_font_weight: 600;
}
