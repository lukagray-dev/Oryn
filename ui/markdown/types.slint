// =============================================================================
// Markdown Type Definitions (`ui/markdown/types.slint`)
// =============================================================================
// Shared struct definitions for inline text formatting spans.

export struct InlineSpan {
    text: string,
    is_bold: bool,
    is_italic: bool,
    is_code: bool,
    is_strikethrough: bool,
}
