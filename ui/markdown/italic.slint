// =============================================================================
// Markdown Italic Token (`ui/markdown/italic.slint`)
// =============================================================================
// Defines global styling tokens and font style parameters for italic text formatting.

export global ItalicTokens {
    in property <bool> italic_font_style: true;
}
