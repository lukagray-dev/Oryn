// =============================================================================
// Markdown Horizontal Rule Formatter (`src/markdown/rule.rs`)
// =============================================================================
// Helper functions for horizontal rule dividers (`---`, `***`, `___`).

/// Returns standard CommonMark horizontal rule markdown syntax (`---`).
pub fn format_rule() -> &'static str {
    "---"
}
